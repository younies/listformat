/**
 * returns list format for all the list formats
 *   input: list of objects in form {
 *			langCode: string,
   			langName: string,
   			listOfItems: list of strings,
 *          }

     return: list of object in form {
	    langCode: string,
    	langName : string,
    	conjunction : string, // conjucate of the listOfItems
    	disjunction: string   // disjuncate of the listOfItems
     }  
 */
getAllListsLanguages = (langList) => { 

   for (var i =  0 ; i < langList.length ; i++) {
   	langList[i]
   }
   

}